<?xml version="1.0" encoding="utf-8"?>
<!--
    自定义历史弹窗内容。原来用 AlertDialog.setItems，是系统默认白底列表，
    在这个纯黑主题下格格不入、也不能高亮当前播放项。改成暗色圆角卡片：
      标题栏 -> 可滚动列表容器(代码里往 list_container 塞行) -> 关闭按钮。
    不引入 RecyclerView（避免加依赖抬高方法数），用 ScrollView + 动态 addView。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_dialog"
    android:padding="20dp">

    <TextView android:id="@+id/dialog_title"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="@string/history_title"
        android:textColor="@color/text_primary" android:textSize="18sp" android:textStyle="bold"/>

    <TextView android:id="@+id/dialog_subtitle"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/text_secondary" android:textSize="12sp"/>

    <View android:layout_width="match_parent" android:layout_height="1dp"
        android:layout_marginTop="14dp" android:background="@color/divider"/>

    <!-- 列表：最多占屏幕一定高度，超出滚动。行由代码动态填充。 -->
    <ScrollView
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:maxHeight="360dp"
        android:scrollbars="none">
        <LinearLayout android:id="@+id/list_container"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical"/>
    </ScrollView>

    <View android:layout_width="match_parent" android:layout_height="1dp"
        android:layout_marginTop="6dp" android:background="@color/divider"/>

    <Button android:id="@+id/dialog_close"
        android:layout_width="match_parent" android:layout_height="44dp"
        android:layout_marginTop="16dp"
        android:text="@string/close"
        android:textColor="@color/text_primary" android:textSize="14sp"
        android:background="@drawable/bg_pill_button"
        android:textAllCaps="false"/>

</LinearLayout>
